# ⚡ What TRUNCATE does
- Removes all data from a table
- Keeps the table, columns, indexes, etc.
- Much faster than DELETE for large data
#### 🧱 Basic syntax
```
TRUNCATE TABLE employees;
```
➡️ This deletes all records from employees, but the table still exists.